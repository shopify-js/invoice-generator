// This is where you write your mutations. 
// Mutations are just like queries except that they modify store data rather than just retrieving it.

const { graphQLClient } = require('./apiClient.js');

module.exports = {

}
